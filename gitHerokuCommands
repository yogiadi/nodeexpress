git init
add in scripts
git add .
git add {file_name}
git commit -m "database changes"
git push heroku master
heroku domains

sudo npm cache clean -f
sudo npm install -g n
sudo n stable

git config --global user.name "user_name"


echo "# nodeexpress" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/yogiadi/nodeexpress.git
git push -u origin master